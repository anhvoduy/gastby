import api from '../api/api';

const getUserList = async () => {
    try {
        return true;
    }
    catch (err) {
        throw err;
    }
};

const getProductList = async () => {
    try {
        return true;
    }
    catch (err) {
        throw err;
    }
};

export default {
    getUserList,
    getProductList
};